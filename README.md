The app only works in a live server because it uses local storage to store and update data

Start the app by opening login page in a live http server. (using "open with live server" command in vscode)